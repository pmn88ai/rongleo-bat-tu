import React from 'react';

/**
 * RecentCustomers Component
 * Đã xóa API call tới /api/consultant/recent — endpoint không tồn tại.
 * Component này render null vì không có data source thay thế.
 * Nếu cần hiển thị lại, phải tạo endpoint tương ứng trước.
 */
const RecentCustomers = () => {
    return null;
};

export default RecentCustomers;
